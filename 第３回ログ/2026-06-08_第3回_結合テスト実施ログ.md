# 第3回 結合テスト 実施ログ（No.1〜No.5）

## 日時
2026-06-08

## 対象テスト項目（仕様書より転記）

| No | 分類 | テスト項目 | 検証内容 |
|---|---|---|---|
| 1 | 正常 | ホームページアクセス | 正常にホームページに入れること |
| 2 | 正常 | タイトル確認 | Analyze Youtube Neighborsが記載があることを確認すること |
| 3 | 正常 | 作者確認 | konpakuzeroの記載があることを確認する |
| 4 | 正常 | 検索 | 検索フォームに「ヒカキン」と記載してボタンを押し、検索結果が出ること |
| 5 | 正常 | 検索 | 検索結果の最初の行の結果を有効化されているアイコンを押して表が表示されること |

- 出典: `D:\ITテスト\test_siyousyo\結合テスト仕様書.xlsx`
- 対象サイト: `https://konpakuzero.pythonanywhere.com/`
- 各項目とも、検証内容に書かれた文言のみを基準に判定する（期待値を推測で拡張しない）
- 1件実施するごとに結果をユーザーへ報告し、次に進んでよいか確認する（[[execute-screen-test-and-log]]準拠）

---

## No.1: ホームページアクセス

### 検証内容
正常にホームページに入れること

### 実施予定の手順
1. `browser_navigate` で対象サイトへアクセス
2. `browser_take_screenshot`（保存先: `第３回ログ/no1_homepage_access.png`）
3. `browser_snapshot` で表示内容を確認
4. `browser_console_messages`（error）でエラー確認
5. 検証内容と照合し判定
6. `browser_close`

### 実施記録
1. `browser_navigate` で `https://konpakuzero.pythonanywhere.com/` にアクセス → 正常に遷移
2. スクリーンショット取得 → `第３回ログ/no1_homepage_access.png`
3. `browser_snapshot` で構造を確認：
   - 見出し「Analyze Youtube Neighbors」「Youtubeチャンネルを分析してみよう」
   - 検索ボックス・検索ボタン、「検索結果」欄、フッター「konpakuzero」
4. コンソールエラー：`favicon.ico` の404エラー1件（前回・前々回と同一事象、表示・機能に影響なし）
5. `browser_close` で終了

### 判定
| 項目 | 内容 |
|---|---|
| 検証内容 | 正常にホームページに入れること |
| 実際の結果 | ページに正常に遷移し、主要コンテンツが表示された |
| 期待値との差異 | なし |
| 判定 | **OK**（前回・前々回と同一結果） |

---

## No.2: タイトル確認

### 検証内容
Analyze Youtube Neighborsが記載があることを確認すること

### 実施予定の手順
1. `browser_navigate` で対象サイトへアクセス
2. `browser_take_screenshot`（保存先: `第３回ログ/no2_title_check.png`）
3. `browser_snapshot` で「Analyze Youtube Neighbors」という文言が画面上に存在するか確認
4. 検証内容と照合し判定
5. `browser_close`

### 実施記録
1. `browser_navigate` で `https://konpakuzero.pythonanywhere.com/` にアクセス → 正常に遷移
2. スクリーンショット取得 → `第３回ログ/no2_title_check.png`
3. `browser_snapshot` で構造を確認した結果、見出し（h1）として
   `heading "Analyze Youtube Neighbors" [level=1]` の表記を確認した
4. `browser_close` で終了

### 判定
| 項目 | 内容 |
|---|---|
| 検証内容 | Analyze Youtube Neighborsが記載があることを確認すること |
| 実際の結果 | 画面見出し（h1）に「Analyze Youtube Neighbors」の記載を確認した |
| 期待値との差異 | なし |
| 判定 | **OK** |

---

## No.3: 作者確認

### 検証内容
konpakuzeroの記載があることを確認する

### 実施予定の手順
1. `browser_navigate` で対象サイトへアクセス
2. `browser_take_screenshot`（保存先: `第３回ログ/no3_author_check.png`）
3. `browser_snapshot` で「konpakuzero」という文言が画面上に存在するか確認
4. 検証内容と照合し判定
5. `browser_close`

### 実施記録
1. `browser_navigate` で `https://konpakuzero.pythonanywhere.com/` にアクセス → 正常に遷移
2. スクリーンショット取得 → `第３回ログ/no3_author_check.png`
3. `browser_snapshot` で構造を確認した結果、ページ下部に
   `generic [ref=e15]: konpakuzero` の表記を確認した
4. `browser_close` で終了

### 判定
| 項目 | 内容 |
|---|---|
| 検証内容 | konpakuzeroの記載があることを確認する |
| 実際の結果 | 画面上に「konpakuzero」の記載を確認した |
| 期待値との差異 | なし |
| 判定 | **OK** |

---

## No.4: 検索

### 検証内容
検索フォームに「ヒカキン」と記載してボタンを押し、検索結果が出ること

### 実施予定の手順（入力操作・ボタン押下を伴う）
1. `browser_navigate` で対象サイトへアクセス
2. `browser_take_screenshot`（テスト開始時、保存先: `第３回ログ/no4_search_before.png`）
3. `browser_snapshot` で検索フォーム（textbox）・検索ボタンの要素を特定する
4. `browser_type` で検索ボックスに「ヒカキン」と入力する
5. `browser_click` で検索ボタンを押下する
6. 画面遷移後に `browser_snapshot` / `browser_take_screenshot`
   （主要画面遷移後、保存先: `第３回ログ/no4_search_after.png`）で検索結果が表示されているか確認する
7. `browser_console_messages`（error）でエラー確認
8. 検証内容と照合し判定
9. `browser_close`

### 実施記録
1. `browser_navigate` で `https://konpakuzero.pythonanywhere.com/` にアクセス → 正常に遷移
2. テスト開始時のスクリーンショット取得 → `第３回ログ/no4_search_before.png`
3. `browser_snapshot` で検索ボックス（`textbox` ref=e9）と検索ボタン（`button "search"` ref=e10）を特定
4. `browser_type` で検索ボックスに「ヒカキン」と入力
   ```js
   await page.getByRole('textbox', { name: '気になるyoutuberの名前やキーワードを入力してください' }).fill('ヒカキン');
   ```
5. `browser_click` で検索ボタンを押下
   ```js
   await page.getByRole('button', { name: 'search' }).click();
   ```
6. 押下後のスクリーンショット取得 → `第３回ログ/no4_search_after.png`
7. `browser_snapshot` で検索結果を確認したところ、「検索結果」欄に表（table）が表示され、
   以下のような行が約20件出力されていた（一部抜粋）：
   - HikakinTV / HikakinGames / HikakinClipTV / HIKAKIN / HikakinBlog /
     HIKAKIN & SEIKIN - Topic / ヒカキン / ヒカキンclip 等
   - 各行にアイコン（リンク）・チャンネル名・チャンネルIDが表示されている
8. `browser_console_messages`（error）→ エラー 0件
9. `browser_close` で終了

### 判定
| 項目 | 内容 |
|---|---|
| 検証内容 | 検索フォームに「ヒカキン」と記載してボタンを押し、検索結果が出ること |
| 実際の結果 | 「ヒカキン」と入力して検索ボタンを押下した結果、「検索結果」欄に約20件のチャンネル一覧（表形式）が表示された |
| 期待値との差異 | なし |
| 発生したエラー | なし（コンソールエラー0件） |
| 判定 | **OK** |

---

## No.5: 検索結果アイコン押下確認

### 検証内容
検索結果の最初の行の結果を有効化されているアイコンを押して表が表示されること

### 実施予定の手順（クリック操作・画面遷移確認を伴う）
1. `browser_navigate` で対象サイトへアクセス
2. `browser_take_screenshot`（テスト開始時、保存先: `第３回ログ/no5_before.png`）
3. No.4と同様に検索ボックスへ「ヒカキン」と入力し、検索ボタンを押下する
4. `browser_snapshot` で検索結果の最初の行（HikakinTV想定）のアイコン（リンク）要素を特定する
5. `browser_click` でそのアイコンをクリックする
6. 画面遷移後に `browser_snapshot` / `browser_take_screenshot`
   （主要画面遷移後、保存先: `第３回ログ/no5_after.png`）で表（table/グラフ等）が表示されているか確認する
7. `browser_console_messages`（error）でエラー確認
8. 検証内容と照合し判定
9. `browser_close`

### 実施記録
1. `browser_navigate` で `https://konpakuzero.pythonanywhere.com/` にアクセス → 正常に遷移
2. テスト開始時のスクリーンショット取得 → `第３回ログ/no5_before.png`
3. No.4と同様に検索ボックスへ「ヒカキン」と入力し、検索ボタンを押下 → 検索結果一覧（約20件）が表示された
4. `browser_snapshot` で最初の行（`row "Icon HikakinTV ..."` ref=e17、内部のアイコンリンク ref=e19、
   リンク先 `/test?channel_name=HikakinTV&channel_id=UCZf__ehlCEBPop-_sldpBUQ`）を特定
5. `browser_click` で最初の行のアイコンをクリック
   ```js
   await page.getByRole('row', { name: 'Icon HikakinTV' }).getByRole('link').click();
   ```
   → URLが `https://konpakuzero.pythonanywhere.com/test?channel_name=HikakinTV&channel_id=UCZf__ehlCEBPop-_sldpBUQ` に遷移した
6. 遷移後のスクリーンショット取得 → `第３回ログ/no5_after.png`
7. `browser_snapshot` で遷移後の画面を確認したところ、以下が表示されていた：
   - 見出し「HikakinTVの分析結果」
   - グラフ表示用のタブ（再生数／高評価数／複合／公開時間）
   - 見出し「チャンネルプロフィール情報」と、その下に**表（table）**が表示されている
     - 表内容: チャンネル名「HikakinTV」、動画投稿数「3,708」、チャンネル登録者数「19,700,000」、
       総再生数「15,647,720,923」、チャンネル創設日時「2011年07月19日11時」
   - 見出し「HikakinTVの動画時間の分析」「動画情報（最大50件）」等
8. `browser_console_messages`（error）→ エラー 0件
9. `browser_close` で終了

### 判定
| 項目 | 内容 |
|---|---|
| 検証内容 | 検索結果の最初の行の結果を有効化されているアイコンを押して表が表示されること |
| 実際の結果 | 最初の行（HikakinTV）のアイコンをクリックすると分析結果ページに遷移し、「チャンネルプロフィール情報」の表が表示された |
| 期待値との差異 | なし |
| 発生したエラー | なし（コンソールエラー0件） |
| 判定 | **OK** |

---

## 全体結果まとめ（フェーズ4）

| No | テスト項目 | 検証内容 | 判定 | スクリーンショット |
|---|---|---|---|---|
| 1 | ホームページアクセス | 正常にホームページに入れること | OK | no1_homepage_access.png |
| 2 | タイトル確認 | Analyze Youtube Neighborsが記載があることを確認すること | OK | no2_title_check.png |
| 3 | 作者確認 | konpakuzeroの記載があることを確認する | OK | no3_author_check.png |
| 4 | 検索 | 検索フォームに「ヒカキン」と記載してボタンを押し、検索結果が出ること | OK | no4_search_before.png / no4_search_after.png |
| 5 | 検索 | 検索結果の最初の行の結果を有効化されているアイコンを押して表が表示されること | OK | no5_before.png / no5_after.png |

### 共通して見られた事象
- 全項目を通じて、ホームページアクセス時に `favicon.ico` の404エラーが発生（ページ表示・機能には影響なし、検証内容にも含まれない事象）

## ステータス
仕様書記載の5件のテスト項目すべてを実施し、いずれも検証内容に照らして問題なし（OK）と判断した。
仕様書（xlsxの「実施日」「結果」列）への結果反映が必要であれば対応する。
